#include <stdio.h>
//
// Created by adhit on 08-02-2026.
//#include<stdio.h>
int main () {
    int a,b;
    scanf("%d %d",&a,&b);
    if (a==b) {
        printf("equal",a);
    }
    if (a!=b) {
        printf("not equal",b);
    }
    return 0;
}